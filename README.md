# Email Validation Using PHPMailer
This repository contains a simple PHP script for validating email addresses using PHPMailer. PHPMailer is a popular library for sending emails via PHP. This script utilizes PHPMailer to check the validity of email addresses through SMTP.

## Requirements
1. PHP version 5.5 or higher
2. PHPMailer library
